[project]
name = "kiro-telegram-bot"
version = "0.1.0"
description = "Telegram Bot with Kiro CLI and AWS Bedrock integration"
requires-python = ">=3.8"
dependencies = [
    "boto3>=1.34.0",
    "requests>=2.31.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
